apiVersion: v1
kind: Service
metadata:
  name: {{ include "urban-test.fullname" . }}
  labels:
    {{- include "urban-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "urban-test.selectorLabels" . | nindent 4 }}
